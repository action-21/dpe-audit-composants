---
import { Enveloppe } from "dpe-audit";
import { IsolationEnveloppe } from "~";
import { Alert } from "./Alert";
import { Card, CardHeader } from "./Card";
import { DialogButton } from "./Dialog";

interface Props {
    performance?: Enveloppe.Performance;
}

const { performance = null } = Astro.props;
---

<Card>
    <CardHeader title="Performance de l'isolation">
        <DialogButton name="performance-isolation" />
    </CardHeader>

    {performance == null && <Alert type="note" content="Non disponible" />}

    {
        performance && (
            <IsolationEnveloppe
                class="max-w-[200px] m-auto"
                performance={performance}
            />
        )
    }
</Card>
